<style>
  .yy-project-content-container{
            margin-top: 130px;
            margin-bottom: 80px;
        }
.yy-tab-container {
    width: 100%;
    margin: 0 auto;
}

.yy-tabs {
    display: flex;
    gap: 44px;
}

.yy-tab {
    padding: 10px 0px;
    cursor: pointer;
    text-align: center;
    transition: all 0.3s;
}

.yy-tab:hover {
    border-bottom: 3px solid #ED1D23;
    font-weight: bold;
}

.yy-tab.active {
    border-bottom: 3px solid #ED1D23;
    font-weight: bold;
}

.yy-tab-content {
    display: none;
    padding: 20px;
    background-color: #f9f9f9;
    margin-top: 10px;
}

.yy-tab-content.active {
    display: block;
}
.yy-project-content-container h1{
  font-weight: 900;
  font-size: 38px;
  line-height: 64px;
}
.yy-tab{
  font-weight: 700;
  font-size: 24px;
  line-height: 25.2px;
}
.yy-tab-image-container{
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 1fr;
    gap: 50px 27px;
}
.yy-tab-image-item img{
    width: 100%;
}
 @media screen and (max-width: 820px){
    .yy-project-content-container{
        margin-top: 40px;
        margin-bottom: 20px;
    }
    .yy-tab-image-container{
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

<div class="yy-projects-content-wrapper section section--page-width">
        <div class="yy-project-content-container">
            <h1>Projects</h1>
            <div class="yy-tab-container">
                <div class="yy-tabs">
                    {% for block in section.blocks %}
                      <div class="yy-tab" id="tab{{ forloop.index }}" onclick="showTab({{ forloop.index }})">{{ block.settings.heading }}</div>
                    {% endfor %}
                </div>
                {% for block in section.blocks %}
                  <div class="yy-tab-content" id="content{{ forloop.index }}">
                      <div class="yy-tab-image-container">
                          {% for i in (1..10) %}
                            {% assign key = "image-" | append: i %}
                            {% assign image = block.settings[key] %}
                              {% if image != blank %}
                                  <div class="yy-tab-image-item">
                                      {{ image | image_url: width: 800 | image_tag  }}
                                  </div>
                              {% endif %}
                          {% endfor %}
                      </div>
                  </div>
                {% endfor %}
            </div>
        </div>
    </div>


<script>
function showTab(tabNumber) {
    const allContent = document.querySelectorAll('.yy-tab-content');
    allContent.forEach(content => {
        content.classList.remove('active');
    });

    const allTabs = document.querySelectorAll('.yy-tab');
    allTabs.forEach(tab => {
        tab.classList.remove('active');
    });

    const activeContent = document.getElementById('content' + tabNumber);
    activeContent.classList.add('active');

    const activeTab = document.getElementById('tab' + tabNumber);
    activeTab.classList.add('active');
}

document.addEventListener('DOMContentLoaded', () => {
    showTab(1);
});

</script>

{% schema %}
{
  "name": "Projects Content",
  "tag": "section",
  "class": "projects-content-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner image"
    }
  ],
  "blocks": [
    {
        "type": "project",
        "name": "Project",
        "settings": [
        {
          "type": "text",
          "label": "Project Title",
          "id": "heading",
          "default": "Title"
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-1",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-2",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-3",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-4",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-5",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-6",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-7",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-8",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-9",
        },
        {
          "type": "image_picker",
          "label": "Image",
          "id": "image-10",
        }
        
      ]
            
    }
  ],
  "presets": [
    {
      "name": "Projects Content"
    }
  ]
}
{% endschema %}